source = ~/.config/hypr/colors.conf

general {
    hide_cursor = true
}

background {
    monitor =
    color = $bg_hard
    blur_passes = 3
    blur_size = 8
    noise = 0.02
    brightness = 0.7
}

input-field {
    monitor =
    size = 380, 70
    outline_thickness = 2
    dots_size = 0.25
    dots_spacing = 0.3
    outer_color = $bg2
    inner_color = $bg0
    font_color = $fg
    fade_on_empty = true
    placeholder_text = <i>haslo...</i>
    check_color = $orange
    fail_color = $red
    fail_text = <i>$FAIL</i>
    halign = center
    valign = center
    position = 0, -80
}

label {
    monitor =
    text = $TIME
    color = $fg_bright
    font_size = 88
    font_family = Inter
    halign = center
    valign = center
    position = 0, 230
}

label {
    monitor =
    text = cmd[update:3600000] date +"%A, %d %B"
    color = $fg_dim
    font_size = 24
    font_family = Inter
    halign = center
    valign = center
    position = 0, 130
}

label {
    monitor =
    text = cmd[update:3000] ~/bin/lock-media-status
    color = $fg_dim
    font_size = 18
    font_family = Inter
    halign = center
    valign = center
    position = 0, 25
}
